# AI Contribution Policy Starter + Audit

Status: synthetic demonstration pack; not activated and not a legal or compliance service.

This bounded pack shows one cautious way to document human-owned decisions about AI-assisted
contributions. Every repository and source is reserved `example.invalid` material. The `$79`
figure is an unvalidated price hypothesis only; there is no listing, checkout, payment route,
customer, order, or promise of availability.

Validate the exact inventory offline from `support-eval-lab/`:

```sh
python -m support_eval_lab.policy_starter policy-starter \
  --json-out samples/policy-starter-validation-receipt.json
```

The receipt proves only deterministic structural and hash consistency for the local synthetic
files. It is not a signature, authentication, authorization, legal review, policy-currentness
check, AI-use detector, compliance result, or enforcement guarantee.
